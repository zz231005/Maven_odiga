<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.odiga.mybatis.login.loginMapper">

	<select id="loginCheck" parameterType="java.util.HashMap" resultType="String">
		${sql }
	</select>
	
	<select id="findId" parameterType="java.util.HashMap" resultType="String">
		${sql }
	</select>
	
	<select id="findPwd" parameterType="java.util.HashMap" resultType="int">
		${sql }
	</select>
	
	<update id="updatePwd" parameterType="java.util.HashMap">
		${sql }
	</update>
</mapper>